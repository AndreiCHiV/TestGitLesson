using System.Collections;
using UnityEngine;

public class TesterMono : MonoBehaviour
{
    private Tester _tester;

    private void Start()
    {
        _tester = new Tester();
    }

    private void Update()
    {
    //    if (Input.GetKeyDown(KeyCode.A))
    //    {
    //        _tester.StartTestRoutine();
    //    }

    //    if (Input.GetKeyDown(KeyCode.D))
    //    {
    //        _tester.StopTestRoutine();
    //    }
    }
}